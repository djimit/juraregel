// JuraRegel SDK — Main entry point

export { JuraregelClient } from "./client.js";
export * from "./types.js";

// Only the independently typed executable domain is exported.
export { GriffierechtClient } from "./griffierecht.js";
